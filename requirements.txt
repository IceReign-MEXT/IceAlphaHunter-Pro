# Core async - Pure Python only
aiohttp==3.9.5
aiosignal==1.3.1
async-timeout==4.0.3
attrs==23.2.0
frozenlist==1.4.1
multidict==6.0.5
yarl==1.9.4

# Telegram - Use v13 (pure Python, no pydantic)
python-telegram-bot==13.15

# HTTP - Pure Python versions
requests==2.31.0
urllib3==2.1.0
certifi==2023.11.17
charset-normalizer==3.3.2

# Environment
python-dotenv==1.0.0

# JSON handling
jsonschema==4.20.0

# Date/time
typing-extensions==4.9.0

# NO pydantic (causes Rust build failures)
# NO httpx (requires pydantic-core)
# NO solders (requires Rust)
# NO solana-py (requires solders)
# NO psycopg2-binary (optional, use SQLite instead)

# SQLite is built-in, no install needed
