# Web Framework
Flask==2.3.3
Werkzeug==2.3.7

# Telegram Bot (sync version - no async needed)
python-telegram-bot==13.7

# Solana (older stable version)
solana==0.25.0
base58==2.1.1
PyNaCl==1.5.0

# HTTP Client (requests is pure Python, no compilation)
requests==2.31.0
urllib3==2.0.7
certifi==2023.7.22
charset-normalizer==3.3.0

# Remove aiohttp - use requests instead
# Remove async dependencies that need compilation

# Environment
python-dotenv==1.0.0

# Database
psycopg2-binary==2.9.9

# Utils
typing-extensions==4.8.0
