# Install in this specific order to avoid conflicts
# Base HTTP stack FIRST
httpcore==0.17.3
httpx==0.24.1

# Telegram (compatible with httpx 0.24.1)
python-telegram-bot==20.4

# WebSocket/Async
websockets==11.0.3
aiohttp==3.9.1

# Database (all compatible with httpx 0.24.1)
postgrest==0.15.0
gotrue==2.1.0
storage3==0.7.0
supafunc==0.3.3
realtime==1.0.2
supabase==2.3.4

# Solana
solana==0.30.2
solders==0.20.0
base58==2.1.1

# Utilities
python-dotenv==1.0.0
requests==2.31.0
pydantic==2.5.3
cryptography==41.0.7
PyJWT==2.8.0
